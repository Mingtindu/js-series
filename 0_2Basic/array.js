const arr = [1,2,3,4];
//Datatype can be mixed in an array:
//Can be change size

const people = ["Starck","Williamson"]
const numberArr = new Array(1,2,5,4,5);

//Methods in an array:

numberArr.push(10);
numberArr.push(20);
numberArr.shift();//remove the value from the beginning of the array
numberArr.unshift(5);//add the value at the beginning of the array:


console.log(numberArr);

//slice and splice 

//slice does not change the value of the original array :
//splice does change the value of the original array:
